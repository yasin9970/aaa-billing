import 'package:flutter/material.dart';

class AppColors {
  // Vyapar Theme Colors
  static const Color primary = Color(0xFF1E3A8A); // Royal Business Blue
  static const Color primaryDark = Color(0xFF0F172A);
  static const Color accent = Color(0xFF2563EB);

  // Accounting Colors
  static const Color saleGreen = Color(0xFF16A34A); // Income / Sale / Paid
  static const Color purchaseBlue = Color(0xFF0284C7); // Expense / Purchase
  static const Color dueRed = Color(0xFFDC2626); // Receivable / Due / Unpaid
  static const Color warningOrange = Color(0xFFEA580C); // Low Stock Alert

  // Background & Surfaces
  static const Color background = Color(0xFFF8FAFC);
  static const Color cardBg = Colors.white;
  static const Color textPrimary = Color(0xFF1E293B);
  static const Color textSecondary = Color(0xFF64748B);
  static const Color border = Color(0xFFE2E8F0);
}
